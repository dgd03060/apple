<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_new"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_new_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar"
        android:usesCleartextTraffic="true">

        <activity android:name=".UserManagementActivity" />
        <activity android:name=".DipsActivity" />
        <activity android:name=".RegisterActivity" /> <!-- http:// 통신일 경우에도 통신 가능하도록 -->
        <activity android:name=".NewsActivity" />
        <activity android:name=".MelonActivity" />
        <activity
            android:name=".Main3Activity"
            android:theme="@style/AppTheme.Pop" />
        <activity android:name=".AlarmActivity" />
        <activity android:name=".Main2Activity" />
        <activity android:name=".NoticeActivity" />
        <activity android:name=".ManagementActivity" />
        <activity android:name=".Newspop"
            android:theme="@style/AppTheme.Pop" />
        <activity
            android:name=".CalenderActivity"
            android:theme="@style/AppTheme.Pop" />
        <activity
            android:name=".NoticePop"
            android:theme="@style/AppTheme.Pop" />
        <activity
            android:name=".Pop"
            android:theme="@style/AppTheme.Pop" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>